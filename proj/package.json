{
  "name": "proj",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 3000",
    "dev:ngrok": "concurrently \"npm run dev\" \"npx ngrok http 3000\"",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "~5.9.3",
    "vite": "^7.1.7"
  }
}